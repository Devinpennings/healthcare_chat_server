#!/bin/bash
sudo docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
sudo docker build -f dockerfile -t $REPO:latest . --build-arg branch=$TRAVIS_TAG
sudo docker tag $REPO:latest $REPO:$TRAVIS_TAG
sudo docker push $REPO